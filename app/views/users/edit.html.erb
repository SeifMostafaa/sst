<h1>Editing User</h1>


<%= form_for(@user, url: user_path, method: :put) do |f| %>

  <%= render partial: "form", locals: { f: f } %>

  <div class="actions">
    <%= f.submit "Update Major" %>
  </div>
<% end %>

